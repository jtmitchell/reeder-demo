"""
App configuration for Reeder.
"""

from django.apps import AppConfig


class ReederConfig(AppConfig):
    """
    App configuration for Reeder.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "reeder"
    verbose_name = "Reeder"
